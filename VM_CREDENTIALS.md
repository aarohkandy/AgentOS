# VM Credentials

**User:** `a_a_k`
**Password:** `1234`

**Root Password:** `ai-os-dev` (default from installer)

## Notes
- User `a_a_k` has sudo access (added manually).
- Shared folder mount point: `/media/sf_AgentOS` (requires `vboxsf` group membership).
